export const environment = {
  production: false,
  apiBaseUrl: 'https://api.example.com', // Replace with your real API base URL
  // Enable mockAuth for local testing without a backend.
  mockAuth: true
};
